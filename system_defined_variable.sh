#!/bin/bash

# system defined variables

echo ${SHELL}
echo ${USER}
echo ${HOME}
echo ${OSTYPE}
echo ${PATH}
echo ${$}  # print process ID
echo ${PPID} # print parant process ID 
#sleep 60
echo ${PWD}
echo ${HOSTNAME}

echo ${UID}
#UID=10000
#echo ${1000}
#PATH=/bin/bash
#echo $PATH

echo ${BASH_VERSION}

sleep 5
echo ${SECONDS}